{
  "name": "zhejiang-art-whitepaper-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "node server.mjs",
    "serve:static": "python3 -m http.server 8787 --directory ."
  }
}
